- name: Atualização de pacotes
  apt:
    update_cache: yes

- name: Instalação de dependências
  package:
    name: "{{ item }}"
    state: present
  loop:
    - apt-transport-https
    - ca-certificates
    - curl
    - software-properties-common
    - git
